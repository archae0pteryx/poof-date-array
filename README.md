# poof-date-array

usage:

```js
const poof = require('poof-date-array')
const arr = poof(start, end, direction) // default direction is forward

```

development:
```js
yarn start
yarn dev
yarn debug
```
